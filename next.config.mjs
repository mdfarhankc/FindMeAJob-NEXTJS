/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "bgylz2rymqjeboww.public.blob.vercel-storage.com",
            }
        ]
    }
};

export default nextConfig;
